import React from "react";
import UserCredentials from "../components/UserCredentials";

function Register(props) {
    return (
        <UserCredentials action={"Register"} />
    )
}

export default Register;
