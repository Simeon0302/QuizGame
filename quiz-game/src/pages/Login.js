import React from "react";
import UserCredentials from "../components/UserCredentials";

function Login(props) {
    return (
        <UserCredentials action={"Login"} />
    )
}

export default Login;
